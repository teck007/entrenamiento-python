---
id: 34
level: 4
type: write
title: "Función que filtra"
xp: 20
hint: "Usa un for con if para filtrar, o una list comprehension."
tests:
  - call: "pares([1, 2, 3, 4, 5, 6])"
    expectedOutput: "[2, 4, 6]"
  - call: "pares([7, 8, 9])"
    expectedOutput: "[8]"
---

Escribe una función llamada "pares" que reciba una lista y devuelva solo los números pares.

```python
def pares(lista):
    # Escribe tu código aquí
```
