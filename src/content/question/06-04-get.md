---
id: 79
level: 6
type: fill
title: "get() con valor por defecto"
xp: 10
hint: "get(clave, default) devuelve el valor de la clave si existe, o default si no."
tests:
  - expectedOutput: "No especificado"
---

Completa el código para obtener el valor de "pais" usando get(). Si no existe, debe devolver "No especificado".

```python
persona = {"nombre": "Ana", "edad": 28}
pais = persona.___
print(pais)
```
