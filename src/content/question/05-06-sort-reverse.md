---
id: 71
level: 5
type: fill
title: "sort y reverse"
xp: 10
hint: "sort() ordena de menor a mayor. reverse() invierte el orden."
tests:
  - expectedOutput: "[5, 4, 3, 2, 1]"
---

Completa el método que invierte el orden de la lista después de ordenarla.

```python
numeros = [3, 1, 4, 2, 5]
numeros.sort()
numeros.___
print(numeros)
```
