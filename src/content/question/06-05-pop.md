---
id: 80
level: 6
type: write
title: "pop en diccionarios"
xp: 20
hint: "pop(clave) elimina la clave y devuelve su valor. Si la clave no existe, puedes pasar un valor por defecto para evitar error."
tests:
  - call: 'quitar({"a": 1, "b": 2, "c": 3}, "b")'
    expectedOutput: "{'a': 1, 'c': 3}"
  - call: 'quitar({"x": 10}, "y")'
    expectedOutput: "{'x': 10}"
---

Escribe la función "quitar" que reciba un diccionario y una clave. Si la clave existe, la elimina con pop(). En cualquier caso, devuelve el diccionario.

```python
def quitar(diccionario, clave):
    # Escribe tu código aquí
```
