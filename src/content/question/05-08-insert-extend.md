---
id: 73
level: 5
type: write
title: "insert y extend"
xp: 20
hint: "insert(pos, elem) agrega en la posición. extend() agrega todos los elementos de otra lista al final."
tests:
  - call: "combinar([1, 2, 3], 1, 99, [4, 5])"
    expectedOutput: "[1, 99, 2, 3, 4, 5]"
---

Escribe la función "combinar" que reciba una lista, un índice, un valor, y otra lista. Debe insertar el valor en el índice con insert(), luego extender con la segunda lista con extend(), y devolver la lista resultante.

```python
def combinar(lista, indice, valor, extra):
    # Escribe tu código aquí
```
